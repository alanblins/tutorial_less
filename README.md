# Sample LESS with Grunt
## Install Grunt
Go  to root folder and install the modules by typing:
```cmd
npm install
npm install -g grunt grunt-cli
```
## Run Grunt
Once installed you can run grunt typing:
```cmd
grunt
```
A new file "style.css" will be created. If you modify the style.less, the style.css will be updated automatically.
